# v2 작업표시줄 산책 모드 — 베타 (v0.17.41)

## 배경

- v1 은 드래그로 옮기는 플로팅 펫. v2 는 **작업표시줄 윗선을 따라 출근개미가 걸어다니고**, 뒤로
  집·회사·업비트/비트겟 타워가 서 있는 "작업표시줄 마을" 표시 모드.
- 그림·도구 파이프라인은 `notes/V2_SPRITE_PIPELINE_NOTES.md` 참조.
- 아직 베타라 **해제 파일이 있는 사용자만** 켤 수 있다 → 우클릭 메뉴 > 표시 스타일 > "작업표시줄 산책 (베타)".
  (해제 방식은 아래 "해제 파일" 섹션)

## 추가/수정

### main.js
1. `walkerWin` — 작업영역 하단에 **모니터 폭 × 340px** 투명 띠(`WALK_STRIP_H`). 바닥 = 작업표시줄 윗선.
   모니터는 오버레이가 있던 모니터(숨겨져도 위치 유지) → 없으면 주 모니터.
   `focusable:false`(포커스 안 뺏음), 기본 `setIgnoreMouseEvents(true, {forward:true})`.
2. `applyDisplayStyle()` / `setDisplayStyle()` — 산책 모드면 산책 창 생성 + **오버레이는 숨김만**
   (오버레이의 시세 WS·트레이 아이콘·무드는 계속 동작). 다른 스타일로 가면 산책 창 destroy + 오버레이 복귀.
   메뉴의 표시 스타일 항목과 `set-setting(displayStyle)` 모두 여기로 모인다.
3. 오버레이 `backgroundThrottling:false` — 숨긴 상태에서도 WS 재연결 타이머·시세 중계가 늦어지지 않게.
4. IPC
   - `walk-interactive`: 산책 창만. 개미 위일 때만 마우스 받기.
   - `walk-toggle-panel`: 클릭한 개미 사각형(화면 좌표)을 `walkAnchor` 로 저장 후 패널 토글.
   - `price`·`pet-test` 중계 대상에 산책 창 추가. `pet-context-menu` 는 보낸 창 기준으로 popup.
   - `set-setting` 가드: 해제 관련 키 직접 설정 금지, 미해제 상태의 `displayStyle=walk` 무시.
5. `anchorPanel()` — 산책 모드면 개미 바로 위 가운데. `togglePanel()` 은 산책 모드에서 저장된 panelPos 무시.
6. `ensureOverlayOnTop()`/`recoverOverlay()` — 오버레이·산책 창 중 **보이는 창** 모두 대상.
   `display-metrics-changed` 에서 `layoutWalker()` 로 새 작업영역에 다시 맞춤.
7. 중복 실행(second-instance) 시 산책 모드면 숨긴 오버레이 대신 산책 창을 앞으로.
8. "펫 보이기/숨기기" 는 산책 모드에서 산책 창 대상.

### 렌더러 (walker.html)
- `renderer/walk/walker-app.js` — 오케스트레이터. BTC 시세는 main 중계(`onPrice`), 무드는
  `mood-controller.js` 재사용(더미 div, 오버레이와 같은 1초 간격 update), 봉 마감은 `candle-scheduler.js`
  재사용(전광판 6초 번쩍), 개발자 테스트 pump/dump/idle/candle → 5초 강제 동작.
  마우스: forward 로 들어오는 mousemove 로 개미 히트테스트 → `walkSetInteractive`. 개미가 걸어서 커서
  밑을 벗어나면 이벤트가 없어도 루프에서 통과로 복귀(빈 곳이 클릭을 먹지 않게). 클릭(5px 미만 이동) →
  패널, 우클릭 → 메뉴.
- `renderer/walk/walk-behavior.js` — 평온: 걷기 5~12초 ↔ 서있기 2~6초, 급등: 계속 달리기,
  급락: 터덜터덜 6~12초 ↔ 서있기 2~4초, 마우스 올리면 멈춤.
- `renderer/walk/walk-scene.js` — 캔버스 그리기. 개미 키 72px, 건물 = heightUnits × 72,
  배치 10/33/64/86%, 전광판은 그림 대비 150% 코드로 덮어 그림. 비트겟 간판 글자는 시세 소스 설정을 따라
  BITGET/BINANCE/HYPERLIQUID 로 바뀜(전광판 가격과 간판 거래소 일치).
- `renderer/walk/upbit-feed.js` — KRW-USDT 웹소켓(바이너리 → TextDecoder, 60초 PING, 백오프 재연결)
  + 첫 값 REST + 환율 5분 주기(야후 → 두나무 → er-api, panel.js 와 같은 순서) → 테더 김프.

### 기타
- 에셋: `img/walk/chulgeun/`(walk·idle·run·trudge + sprites.js), `img/town/`(home·office·upbit·bitget + town.js).
  JSON fetch 대신 `.js` 매니페스트(파일 프로토콜에서 안전).
- preload.js — `walkSetInteractive`.
- package.json — 0.17.40 → **0.17.41**, `build.files` 에 `!art-src/**`, `!tools/**` (원본 그림·개발 도구 설치 파일 제외).

## 불변사항 / 주의

- **v1 오버레이 코드·CSS·무대 기준선은 변경 없음.** 산책 모드는 오버레이를 숨길 뿐 파괴하지 않는다
  (시세 WS 소유자이자 트레이 아이콘 갱신 담당).
- 산책 띠 높이(`WALK_STRIP_H`)는 개미·마을 축소·전광판 배율로 계산한 값. 크기 설정을 추가하면 같이 조정할 것
  (아래 후속 섹션에서 340 → 230).
- 투명 띠는 클릭 통과지만 **건물이 선 자리는 화면을 가린다**. 건물 숨기기/크기 설정은 미구현.
- 검증 범위: 산책 창 렌더러는 가짜 btcpet API 하네스로 헤드리스 렌더링 확인(오류 없음, 건물·개미·전광판 표시),
  업비트 웹소켓 파싱은 Node 로 실연결 확인. **Electron 실제 실행(창 위치·클릭 통과·패널 위치·메뉴)은 미검증.**
- dev 실행(`npm start`)은 설치된 BTC Pet 과 **같은 userData(settings.json)와 단일 인스턴스 락**을 쓴다
  → 설치본을 종료한 뒤 실행해야 하고, 해제·표시 스타일 설정이 설치본에도 남는다.

## 후속: 마을 축소 + 왼쪽 모음 배치 (같은 0.17.41, 미배포)

### 원인
- 사용자 실행 결과(2560px 모니터): 거래소 타워 288px 가 작업 중 화면을 너무 가림.
- 화면 폭 비율 배치(10·33·64·86%) 때문에 넓은 모니터에서 **가리는 곳이 네 군데로 흩어짐**.

### 수정 (walk-scene.js, main.js)
1. `TOWN_SCALE = 0.65` — 건물 높이 = heightUnits × 72 × 0.65 (거래소 약 187px). 개미는 72px 유지.
2. 전광판은 건물만큼 줄이면 글씨가 안 읽혀 `BOARD_SCALE = 1.5 × 0.85 / 0.65` 로 역보정
   → 축소 전 150% 전광판의 85% 크기 유지. 테두리 두께도 전광판 배율에 비례.
3. 배치: 비율 배치 제거 → **왼쪽 구석부터 집·회사·업비트·비트겟을 붙여 모음**
   (여백 12px, 간격 14px, 전광판이 타워보다 넓으면 전광판 폭을 차지 폭으로 계산해 겹침 방지).
   오른쪽 구석은 윈도우 알림 팝업 자리라 제외, 양 끝 분리(출근길)는 가리는 곳이 두 군데라 보류.
4. `geometry()` 로 건물 크기·전광판 사각형 계산을 한곳에 모음(draw·layout·requiredHeight 공용).
5. `WALK_STRIP_H` 340 → **230** (계산상 필요 높이 약 205px).

### 불변사항 / 주의
- 개미가 건물 문보다 커 보인다(장난감 마을 느낌). 나중에 건물 드나들기를 넣을 땐 문 크기 재검토.
- 타워 간판 글자(UPBIT/BITGET)는 축소로 약 5px 라 거의 장식 수준.
- tools/walk-preview.html 은 아직 예전 비율 배치·크기라 앱과 다르다(앱 기준은 walk-scene.js).

## 후속: 산책 모드에서 개미 클릭 → 패널 열기 제거 (같은 0.17.41, 미배포)

### 원인
- 개미 위로 패널이 튀어나오면 작업 화면을 또 가림 → 사용자 요청으로 산책 모드에선 막음.

### 수정
1. walker-app.js — 좌클릭 판정(mousedown/mouseup)·`walkTogglePanel` 호출 삭제. 호버 시 멈춤·우클릭 메뉴는 유지.
2. preload.js / main.js — `walkTogglePanel` / `walk-toggle-panel` IPC, `walkAnchor` 삭제.
3. `anchorPanel()` — 산책 모드면 산책 띠 모니터의 **오른쪽 아래(트레이 근처)**. 마을이 왼쪽 구석이라 겹치지 않음.
   `togglePanel()` 은 산책 모드에서 저장된 panelPos 대신 항상 이 위치.

### 불변사항
- 산책 모드의 패널 진입 경로 = **트레이 아이콘 클릭**, **우클릭 메뉴 > 패널 열기/닫기**. v1 펫 클릭 → 패널은 그대로.
- 위 1차 섹션의 `walk-toggle-panel`/`walkAnchor` 설명은 이 후속으로 대체됨.

## 후속: 마을 좌우 위치 설정 (같은 0.17.41, 미배포)

- 설정 `walkTownSide`: `left`(기본) / `right`. 우클릭 메뉴 "산책 마을 위치"(해제된 경우만 노출).
- walk-scene.js `layoutTown()` — 묶음 전체 폭을 계산해 오른쪽이면 오른쪽 끝(여백 12px)에 맞춤.
  오른쪽이어도 집→회사→업비트→비트겟 순서 유지.
- main.js `setWalkTownSide()` — 저장·broadcast 후 패널이 열려 있으면 재배치. 산책 모드 패널은
  **마을 반대편 아래**(마을 왼쪽 → 패널 오른쪽 아래, 마을 오른쪽 → 패널 왼쪽 아래).
- 오른쪽은 윈도우 알림 팝업 자리와 겹칠 수 있어 메뉴 문구에 안내.

## 해제 파일 (같은 0.17.41, 미배포)

- 산책 모드는 개인별 해제 파일(`.btcpet`)로 켠다. 발급·키 관리는 저장소 밖에서 하며,
  **그 경로·절차·한계를 저장소 파일(코드 주석·노트·CLAUDE.md)에 적지 않는다.**
- 적용 경로: 펫·패널 창에 **끌어다 놓기**(preload.js 가 공통 처리, 드롭 시 페이지 이동 기본 동작도 막음),
  **더블클릭**(package.json `build.fileAssociations` → 앱 실행 인자 / `second-instance` argv).
- 성공 시 "OO님, 산책 모드가 열렸어요 [지금 켜기/나중에]" 다이얼로그 + 패널 하단 `🐜 산책 모드 · 이름님`.
- `get-settings` 는 해제 원문 대신 `walkUnlocked`·`walkLicenseName` 만 넘긴다. 해제 키 직접 설정 금지.
- .gitignore — `*.btcpet`.
- 주의: 더블클릭은 파일 연결이 등록된 **설치본**에서만 동작(dev 는 드롭으로 테스트). 클릭 통과를 켠 펫 창에는
  드롭이 안 될 수 있음 → 패널에 드롭.

## 해제 파일 형식 v2 · 사용 기간 (0.17.42, 미배포)

- 원인: 공개키 하나·영구 파일만 지원 → 여러 서명 키와 기간제 파일이 필요.
- 수정 (main.js):
  - `WALK_LICENSE_PUBLIC_KEYS` = `{ kid: PEM }`. v1 파일은 `p1` 키로 검증(기존 파일 그대로 유효).
  - v2 필드: `kid`, `tg`(빈 문자열 가능), `exp`(마지막 사용 가능일 `YYYY-MM-DD`, 빈 문자열 = 기간 없음).
    서명 문자열 = `["btcpet-unlock","v2",kid,feature,id,name,tg,issuedAt,exp].join("\n")` (`LICENSE_FIELDS`).
  - `exp` 는 로컬 날짜로 비교, 그날까지 사용 가능. 기간이 끝난 파일을 넣으면 "사용 기간이 끝난 해제 파일" 안내.
  - `isWalkUnlocked()` 캐시는 날짜가 바뀌면 다시 검증. `recheckWalkLicense()` 10분 주기 —
    산책 중 기간이 끝나면 산책 창을 닫고 기본 표시로(설정 `displayStyle` 은 그대로, 새 파일을 넣으면 복귀).
  - 저장은 `pickLicense()` 로 버전별 필드만. `get-settings` 에 `walkLicenseExp` 추가 → 패널 `🐜 산책 모드 · 이름님 · 날짜까지`.
- 불변: v1 파일 형식·검증 결과 변화 없음. 해제 원문은 렌더러로 넘기지 않음.
- 검증(Node): v2 영구/기간 내 통과, 만료·exp/tg/kid 조작·v1→v2 위장 거부, 기존 v1 파일 통과.

## 산책을 못 쓰게 됐을 때 산책 전 스타일로 복귀 (0.17.43, 미배포)

- 원인: 기간이 끝나도 저장값 `displayStyle` 은 `walk` 로 남는다(새 파일을 넣으면 복귀하려고).
  그래서 우클릭 메뉴 표시 스타일에 체크가 하나도 없었고, 오버레이는 재시작하면 산책 전 스타일과 상관없이 기본값 펫으로 떴다.
- 수정:
  - main.js `rememberPrevStyle()`: 산책으로 바꿀 때(`setDisplayStyle`·`set-setting`) 직전 스타일을 `walkPrevStyle`(pet/pill)에 저장.
  - `baseStyle()`: 저장값이 walk 면 `walkPrevStyle`, 아니면 저장값 (둘 다 pill 이 아니면 pet).
  - 메뉴 체크: 산책 중이면 산책, 아니면 `baseStyle()` 에 체크.
  - `recheckWalkLicense()`: 산책 창을 닫은 뒤 오버레이에 `displayStyle = baseStyle()` 을 방송(저장값은 walk 그대로).
  - overlay-app.js: 시작 시 저장값이 walk 면 `walkPrevStyle` 로 스타일 결정.
- 불변: 저장값 walk 유지 → 새 해제 파일을 넣으면 산책으로 복귀. `walkPrevStyle` 이 없는 기존 산책 사용자는 펫.
- 검증(Node): pet/pill→산책→만료, 산책 중 재선택, 산책→펫→산책, 기존 사용자 6가지 경우 메뉴·오버레이 스타일 일치.

## 우클릭 메뉴를 표시 모드별로 (0.17.44, 미배포)

- 원인: 메뉴가 모드와 상관없이 전부 보여서, 산책 모드에 펫 캐릭터·펫 크기 같은 쓸모없는 항목이 보이고
  펫 모드에도(해제된 사용자) 마을 위치·산책 범위가 보였다.
- 수정 (main.js `buildMenu()`): `mode = isWalkMode() ? "walk" : baseStyle()` 로 항목을 고른다.
  | 모드 | 보이는 전용 항목 |
  |---|---|
  | walk | 마을 설정, 산책 캐릭터(2명 이상), 마을 위치, 산책 범위, 산책할 모니터·넘어다니기(2대 이상), 마을을 다른 창 위에 표시 |
  | pet | 패널 항상 표시·붙여다니기·위치 초기화, 캐릭터, 펫 크기, 차트 이펙트, 클릭 통과 |
  | pill | 패널 항상 표시·붙여다니기·위치 초기화, 클릭 통과 (펫 무대가 숨겨져 캐릭터·크기·이펙트는 효과 없음) |
  - 공통: 패널 열기/닫기, 거래소, 표시 스타일, 개발자 모드, 보이기/숨기기, 새로고침, 부팅 자동 실행, 업데이트, 버전, 종료.
  - 산책 모드에선 "개미·마을 보이기/숨기기", "개미·마을 새로고침" 으로 이름만 바꿈(동작은 기존 그대로 산책 창 대상).
- 불변: 각 항목의 동작·설정 키는 그대로. 숨긴 항목의 저장값도 유지(모드를 바꾸면 다시 보임). 트레이 우클릭도 같은 `buildMenu()`.
- 검증(Node, Menu 스텁): 산책(모니터 2대) / 펫 / 기본형(미해제) / 기간 끝난 산책 저장값(산책 전 기본형) 4경우 항목 목록 확인.

## 후속: 개미 산책 범위 제한 (같은 0.17.41, 미배포)

- 설정 `walkRange`: `town`(마을 묶음 + 개미 3마리 폭 바깥까지) / `half`(마을 쪽 화면 절반) / `full`(기본, 화면 전체).
  우클릭 메뉴 "산책 범위"(해제된 경우만).
- walk-scene.js `walkRange()` — layoutTown 이 기록한 `townExtent` 와 `townSide` 로 [minX, maxX] 계산.
- walk-behavior.js `update({minX, maxX})` — 가장자리 되돌아서기를 범위 기준으로. 설정 변경 직후 범위 밖이면
  순간이동하지 않고 무드에 맞는 이동 동작으로 **범위 안까지 걸어 들어온다**.
- 헤드리스 하네스(마을 오른쪽 + town)에서 개미가 마을 구간 안에 머무는 것 확인.

## 후속: 창 너비 축소 · 창 겹침 단계 · 트레이 클릭 (같은 0.17.41, 미배포)

### 원인
- 산책 창이 범위와 상관없이 항상 모니터 전체 폭이었다.
- 산책 창이 v1 오버레이와 같은 "screen-saver" 단계라 작업표시줄보다 위 → 알림·작업표시줄 쪽 창을 가릴 수 있음.
- 패널이 열린 상태에서 트레이 아이콘을 누르면 blur 로 먼저 숨고 곧바로 click 이 다시 열어서 **닫히지 않음**.
  산책 모드에선 패널이 트레이와 떨어진 구석에 뜸.

### 수정
1. **창 너비** — 렌더러가 `requiredSpan()`(산책 범위 ∪ 마을·전광판 ∪ 현재 개미 위치, 32px 단위)을 `walk-span` 으로
   보내면 main 이 그 구간만큼 창을 자르고 `walk-viewport {fullWidth, offset}` 회신. 렌더러는 항상 작업영역 전체 폭
   좌표로 계산하고 `offset` 만큼 밀어서 그림(마우스도 +offset). 전체 폭을 받기 전엔 요청하지 않음(창 폭을 전체 폭으로
   오인해 계속 줄어드는 루프 방지). 범위가 "화면 전체"면 전체 폭.
2. **겹침 단계** — 산책 창 `setAlwaysOnTop(true, "floating")`: 일반 창보다 위, 작업표시줄 쪽 창보다 아래
   (Electron: floating~status 는 Windows 작업표시줄 아래, pop-up-menu 이상은 위). v1 오버레이는 기존 screen-saver 유지.
   메뉴 "마을을 다른 창 위에 표시"(settings.walkOnTop, 기본 켬) 끄면 일반 창처럼 다른 창 뒤로 감.
   20초 최상위 재선언도 walkOnTop 일 때만, floating 단계로.
3. **트레이** — blur 로 숨긴 시각을 기록해 300ms 안의 트레이 click 은 무시(닫기 동작). 산책 모드 패널은
   `tray.getBounds()` 기준 **트레이 아이콘 바로 위**(못 얻으면 마을 반대편 구석).

### 검증 / 주의
- 헤드리스 하네스(가짜 main 이 span 에 맞춰 캔버스를 자르고 viewport 회신): 마을 왼쪽+주변 → 창 0~864px,
  마을 오른쪽+주변 → 창 736~1600px, 두 경우 모두 건물·개미가 원래 전체 폭 위치에 그려지고 오류 없음.
- **floating 단계에서 윈도우 알림 토스트·시작 메뉴가 실제로 위에 뜨는지는 Electron 실행으로 확인 필요.**
  부족하면 "다른 창 위에 표시"를 끄는 것이 확실한 대안.
- 트레이 아이콘이 숨은 아이콘(^) 영역에 있으면 getBounds 가 비어 구석 배치로 폴백.

## 후속: 개미 호버 커서 (같은 0.17.41, 미배포)
- 원인: 좌클릭 → 패널을 없앤 뒤에도 호버 시 커서를 `pointer`(손가락)로 바꿔 클릭되는 것처럼 보였음.
- 수정: walker-app.js `setInteractive()` 에서 커서 변경 삭제 → 기본 화살표. 호버 시 멈춤·우클릭 메뉴는 유지.

## 후속: 마을 끌어서 옮기기 (같은 0.17.41, 미배포)

### 원인
- 좌/우 끝 두 가지로는 모니터마다 비어 있는 자리에 맞추기 어려움 → 작업표시줄 위에서 가로로 자유 배치(세로 이동은 제외).

### 수정
1. 메뉴 "산책 마을 위치(좌/우)" → **"마을 위치"**: `끌어서 옮기기…`(산책 모드일 때만) / `왼쪽 끝으로` / `오른쪽 끝으로`.
   끝으로 보내기는 `walkTownPos` 를 지우고 `walkTownSide` 프리셋 사용.
2. 설정 `walkTownPos` = 마을 중심 / 작업영역 전체 폭 **비율**(해상도가 바뀌어도 비슷한 자리). 있으면 프리셋보다 우선.
   walk-scene.js `layoutTown()` 이 화면 밖으로 안 나가게 클램프.
3. 옮기기 모드(walker-app.js): main 이 `walk-move-mode` 전송 → 창 전체 폭 + 마우스 받기. 마을(전광판 포함 영역)을
   누르고 끌면 `townPos` 실시간 갱신, 커서 grab/grabbing, 점선 테두리 + 안내 문구.
   **놓으면** `setSetting("walkTownPos")` 저장 후 종료, **마을 바깥 클릭**·**30초 무동작**은 옮기지 않고 종료,
   끄는 중 창 밖으로 나가면 그 자리에서 고정. 산책 창은 포커스를 받지 않아 Esc 는 쓰지 않음.
4. "마을 쪽 절반" 범위·패널 폴백 위치는 좌/우 프리셋 대신 **실제 마을 중심이 화면 절반 어느 쪽인지**로 판단.

### 검증
- 헤드리스 하네스로 가짜 드래그(300px → 900px): 드래그 중 창 전체 폭·점선·"놓으면 이 자리에 고정" 표시,
  놓은 뒤 마을이 옮긴 자리에 남고 창이 다시 마을+범위 구간으로 줄어듦. 오류 없음.
- 실제 마우스·Electron 창에서의 드래그 감각은 미검증.

## 후속: 마을 옮긴 뒤 흔들림 (같은 0.17.41, 미배포)

### 원인
1. 창 구간에 개미 위치(±72px)를 넣었는데 범위 경계보다 개미 몸이 약 29px 더 나가서, 개미가 가장자리 근처를 걸을
   때마다 32px 단위 구간이 바뀜 → **창이 반복해서 크기·위치 변경**.
2. 창 위치가 바뀐 직후 main 의 `walk-viewport`(offset)가 오기 전 프레임이 옛 offset 으로 그려져 **마을 전체가 순간적으로 튐**.

### 수정
1. walk-scene.js `requiredSpan()` — 산책 범위에 개미 몸 폭(ANT_PX)을 **항상** 더해 범위 안에서 걷는 동안 구간 불변.
   개미가 범위 밖일 때만 개미 위치까지 넓힘.
2. walker-app.js `syncSpan()` — 개미가 범위 밖에서 걸어 들어오는 중엔 **줄이는 요청을 보내지 않음**(들어온 뒤 한 번만).
3. `syncOffset()` — 매 프레임 `window.screenX - workAreaX` 로 offset 계산(main 이 viewport 에 `workAreaX` 추가).
   메시지 도착을 기다리지 않으므로 창 이동 순간의 어긋남 제거.

### 검증
- 헤드리스 하네스(마을 드래그 후 40초): 창 구간 요청 1회(드롭 직후)뿐, 이후 개미가 걸어도 변경 없음. 오류 없음.

## 후속: 창 크기 변경 순간 번쩍임 (같은 0.17.41, 미배포)

### 원인
- 창을 옮기거나 크기를 바꾸면(setBounds) 윈도우가 **마지막으로 그려진 프레임을 새 위치에 한 번 보여 준 뒤** 새 프레임이
  올라온다. offset 을 매 프레임 다시 계산해도 이 한 프레임은 렌더러에서 막을 수 없음 → 마을을 놓은 직후 한 번 번쩍.

### 수정 (walker-app.js)
- 창 구간 변경을 바로 보내지 않고 `requestResize()` → 장면 페이드: **out 120ms → clear(빈 프레임 1장) → 창 변경 →
  settle 150ms → in 200ms**. 창을 바꾸는 시점에 화면에 올라가 있는 프레임이 빈 화면이라 옮겨 보일 게 없다.
- 첫 창 배치는 보여 준 적이 없으므로 페이드 없이 바로 맞추고, 첫 배치 전(viewport 수신 전)에는 그리지 않음.
- walk-scene.js `draw({alpha})` — alpha 0 이면 지우기만, 그 외엔 캔버스 opacity 로 적용.
- 창 구간 변경은 마을 옮기기·산책 범위 변경·옮기기 모드 진입/종료 때만 일어나므로 평소엔 페이드가 보이지 않음.

### 검증
- 헤드리스 가상 시간 모드는 requestAnimationFrame 이 거의 돌지 않아 타이밍 측정 불가(마지막 그리기 3.4초 전) → 부적합.
- 대신 페이드 코드를 그대로 추출해 Node 에서 16ms 프레임으로 시뮬레이션: 1.0→0 으로 사라진 뒤, **직전에 그린 프레임의
  투명도가 0 인 상태에서만** 창 변경 호출, 이후 0.2초에 걸쳐 1.0 복귀. (clear 단계 추가 전엔 직전 프레임 9% 잔상 가능성 있었음)
- 실제 윈도우에서 번쩍임이 사라졌는지는 Electron 실행으로 확인 필요.

## 후속: 건물 위 마우스 → 건물 반투명 (같은 0.17.41, 미배포)
- walker-app.js `updateTownAlpha()` — 옮기기 모드가 아니고 커서가 마을(전광판 포함) 위면 목표 투명도 0.18, 아니면 1.
  프레임마다 `dt×12` 비율로 따라가 약 0.15초 전환. walk-scene.js `draw({townAlpha})` 가 건물만 globalAlpha 적용(개미는 불투명).
- 창은 원래 클릭 통과라 반투명 상태에서 뒤 창을 그대로 클릭할 수 있다.
- 헤드리스 캡처는 rAF 타이밍을 재현하지 못해 미검증(실행 확인 필요).

## 후속: 창 너비 축소 되돌림 — 항상 작업영역 전체 폭 (같은 0.17.41, 미배포)

### 원인
- 창 너비를 마을+산책 범위로 줄이는 기능 때문에 창 크기가 바뀔 일이 계속 생김(마을 옮기기 시작·놓기, 개미가 범위 안으로
  들어올 때). 크기가 바뀔 때마다 번쩍임 → 페이드로 가리자 이번엔 "중간에 갑자기 투명해지는 단계"로 보임.
- 투명 창은 그려진 부분 외엔 아무것도 가리지 않고 클릭도 통과하므로 너비를 줄여 얻는 이득이 거의 없음.

### 수정
- main.js: `walkSpan`, `walk-span` IPC, `walk-viewport` 전송 제거. `walkBounds()` 는 항상 작업영역 전체 폭,
  크기 변경은 모니터/작업영역이 바뀔 때(`layoutWalker`)만.
- preload.js: `walkSetSpan`, `onWalkViewport` 제거.
- walk-scene.js: `fullWidth`/`offset`/`logicalWidth`/`requiredSpan()`/`draw({alpha})` 제거 → 창 폭 = 배치 기준 폭.
- walker-app.js: `syncOffset`/`syncSpan`/페이드 상태머신 제거, 마우스 좌표 보정(+offset) 제거.
- 위의 "창 너비 축소", "옮긴 뒤 흔들림", "창 크기 변경 순간 번쩍임" 섹션의 너비·offset·페이드 관련 내용은 이 섹션으로 대체됨.
  (산책 범위 제한 `walkRange` 와 범위 밖에서 걸어 들어오기는 유지)

### 함께 고친 버그
- 마을을 끈 뒤 한 번도 다시 그려지기 전에 놓으면 `townExtent` 가 갱신 안 된 채 **옮기기 전 위치가 저장**됨
  → mousedown·놓기 시점에 `scene.layoutTown()` 을 먼저 호출. 하네스(300→900px 드래그)에서 저장값 0.197(옛 위치) → 0.577(옮긴 위치).

## 후속: 산책할 모니터 선택 (같은 0.17.41, 미배포)

### 원인
- 산책 모니터가 "숨긴 v1 오버레이가 마지막으로 있던 모니터"로만 정해져, 다른 모니터로 옮기려면 펫 모드로 바꿔 펫을
  끌어 옮긴 뒤 다시 산책 모드로 돌아와야 했음.

### 수정 (main.js)
- `walkDisplay()`: `settings.walkDisplayId` 로 고른 모니터 → 없으면(미선택·연결 해제) 오버레이 모니터 → 주 모니터.
- 우클릭 메뉴 **"산책할 모니터"**(모니터 2대 이상일 때만): 왼쪽→오른쪽 순서로 `모니터 N (주 모니터) · 가로×세로`, 라디오.
  고르면 저장 → `layoutWalker()` → 패널이 열려 있으면 재배치. 마을 위치는 비율 저장이라 새 모니터 폭에 맞게 따라감.
- `display-added` / `display-removed` 에서도 `layoutWalker()` → 고른 모니터를 뽑으면 자동으로 대체 모니터로 이동.

### 검증
- walkDisplay/walkDisplayMenu 코드를 추출해 가짜 screen(2560×1440 주 모니터 + 1920×1080)으로 확인:
  미선택 → 주 모니터, 메뉴 "모니터 1 (주 모니터) · 2560×1440 ✓ / 모니터 2 · 1920×1080", 선택 시 해당 작업영역,
  선택 모니터 해제 시 주 모니터로 대체, 모니터 1대면 메뉴 숨김.
- 실제 멀티모니터 Electron 실행은 미검증.

### 보류
- 개미가 모니터 경계를 넘어 옆 모니터로 걸어가기(모니터별 창·해상도/배율/작업표시줄 높이 차이 처리 필요).

## 후속: 회사 이름 · 건물 숨기기/순서 (같은 0.17.41, 미배포)

- 패널 ⚙ 설정에 **"산책 마을"** 섹션(`#townBox`, 해제된 사용자만 표시).
  - 회사 이름(`walkCompanyName`, 10자): 입력 멈추고 0.4초 뒤 저장, 빈 칸이면 기본 "(주)개미상사".
  - 건물 목록: 체크 해제 = 숨김(`walkTownHidden`), ▲▼ = 왼쪽→오른쪽 순서(`walkTownOrder`).
    모르는 id 는 버리고 새 건물은 뒤에 붙이는 정규화를 패널·산책 창 양쪽에서 수행.
- walk-scene.js `townOrder`(보이는 건물만) 로 배치·높이 계산. 전부 숨기면 마을 폭 0 → 호버·옮기기 판정 없음.
- 하네스: "코인상사" + [비트겟, 업비트, 회사] + 집 숨김으로 그려짐, 패널 섹션 표시 확인.
- **건물 추가**는 새 그림이 필요해 보류(후보: 바이낸스 타워·카페·편의점·은행 등 → 같은 파이프라인으로 town_slice).

## 후속: 개미 모니터 넘어다니기 (같은 0.17.41, 미배포)

### 구조
- 마을 모니터엔 `walkerWin`(마을+개미), 나머지 모니터엔 **개미 전용 창** `walkSatellites`(displayId → 창).
  메뉴 "개미가 모니터 넘어다니기"(`walkCrossMonitors`, 모니터 2대 이상일 때만) 켤 때만 만든다.
- main 이 `antDisplayId`(개미가 있는 모니터)를 소유하고 각 창에 `walk-role` 전송:
  `{home, hasAnt, canCross:{left,right}, enter:{side,action}|null}`. 창 로드 완료(`did-finish-load`) 때마다 재전송.
- `crossTarget(display, side)`: 그 방향 가장자리에 2px 이내로 붙고 세로로 겹치며 산책 창이 있는 모니터(위·아래 배치는 제외).
- 렌더러: 가장자리에 닿으면 `canLeave` = (산책 범위 "화면 전체" && 그쪽 canCross && 넘어온 지 5초 경과 && 50%) →
  되돌아서지 않고 화면 밖까지 걸어 나감(`exiting`) → 완전히 나가면 `walk-ant-leave` → main 이 옆 창에 hasAnt+enter.
  옆 창은 `behavior.enter()` 로 반대편 화면 밖에 세우고 "범위 밖이면 걸어 들어오기" 규칙으로 들어옴.
  넘어갈 곳이 없으면 main 이 원래 창에 hasAnt 를 다시 보내 그 자리에서 되돌아옴.
- 역할을 받기 전엔 아무것도 안 그림(개미 전용 창에 마을·개미가 잠깐 보이는 것 방지). 업비트 피드·옮기기 모드는 마을 창만.
  개미 전용 창의 산책 범위는 항상 화면 전체.
- 모니터 해제로 개미가 있던 창이 사라지면 마을 모니터로 복귀. 마을 모니터를 바꾸면 개미도 마을로.
- 여러 창 대응: 시세·설정·개발자 테스트 브로드캐스트, 클릭 통과 전환, 최상위 재선언·복구, 다른 창 위 표시, 보이기/숨기기,
  중복 실행 시 표시가 모든 산책 창 대상.

### 검증 (Node 시뮬레이션, 실제 멀티모니터 미검증)
- main 블록 추출 + 가짜 모니터 [1][2 마을][3] + 위쪽 [4]: 역할 1=오른쪽만, 3=왼쪽만, 4=넘어갈 곳 없음.
  2→오른쪽 나감 → 3 hasAnt+enter(left,run) / 개미 없는 창의 신호 무시 / 오른쪽 끝 3에서 나감 → 거절·복귀 /
  3→2→1 이동 / 1 모니터 해제 → 마을로 복귀 / 넘어다니기 끔 → 보조 창 제거.
- behavior 추출: 넘어갈 수 있으면 가장자리(956.8)에서 나가기 시작 → x=1044 에서 leaving, 없으면 되돌아섬,
  enter 후 61프레임에 화면 안, 평온 중 넘어와도 걸어 들어옴.

## 후속: 산책 설정 창 · 거래소 질주 · 말풍선 · 가격 알림 · 밤 모드 · 반투명 전광판 비침 (같은 0.17.41, 미배포)

### 산책 설정 창 (패널에서 이동)
- 원인: 산책 모드에선 패널을 쓰지 않는데 회사 이름·건물 설정을 패널 ⚙ 에 넣었음 → 패널의 "산책 마을" 섹션 제거.
- `walk-settings.html/js` + main `openWalkSettings()`: 우클릭 메뉴 **"마을 설정… (이름·건물·가격 알림·밤 모드)"**.
  400×860 일반 창(크기 조절 가능). 섹션: 회사 이름 / 건물 보이기·순서 / 가격 알림 / 밤 모드 / 개미(말풍선·질주 켜기).
  main 은 이 창에도 `setting-changed` 브로드캐스트와 BTC 현재가(`price`)를 보냄. 테더 현재가는 창에서 30초마다 REST 조회.

### 반투명 때 작은 전광판 비침
- 원인: 건물마다 globalAlpha 로 그리면 코드 전광판도 반투명해져 그 아래 그림 속 작은 전광판이 비침.
- 수정: townAlpha < 1 이면 마을을 보조 캔버스(`townLayer()`)에 불투명하게 그린 뒤 한 번에 반투명으로 얹음.

### 밤 모드
- 새 그림 없이 `tools/town_slice.py --night` 가 `{name}_night.png` 생성: 유리창(B≥236, G≥190, 60≤R≤165)은 따뜻한 노랑,
  나머지는 어둡고 푸르게. 비트겟 타워는 벽 청록이 유리와 같은 색이라 `--night-mask-from bitget=upbit` 로
  같은 크기·같은 모양인 업비트 타워의 창문 위치를 빌림. meta `nightImage`.
- walk-scene.js: `night` 이면 밤 그림, 간판 글자 밝게(#f3e6c4), 전광판 테두리 55% 밝기(화면 글자는 그대로).
- 설정 `walkNight`: auto(19시~6시) / day / night. 10초마다 판정.

### 거래소 질주
- walk-behavior.js `setGoal(x, ms)` / `clearGoal()`: 목표까지 run, 6px 이내면 idle 로 서서 전광판 구경, 지나치지 않게 클램프,
  도착 순간 `arrived`. 우선순위: 마우스 호버·화면 밖 나가기·범위 밖 복귀 > 목표 > 개발자 테스트 > 무드 리듬.
- walker-app.js: 무드가 pump/dump 로 바뀌면 비트 거래소 타워(숨겼으면 업비트 타워) 앞으로. 유지 시간 = 거리/달리기 속도 + 6초.
  산책 범위 밖 타워면 범위 끝까지만. 마을 창에 개미가 있을 때만, 목표 중엔 모니터 넘어가지 않음. 설정 `walkDash`.
- 개발자 테스트 Pump/Dump 도 같은 흐름(말풍선+질주)으로 확인 가능.

### 말풍선
- walk-scene.js `drawBubble()`: 개미 머리 위 흰 둥근 상자+꼬리, 화면 끝에선 상자만 안쪽으로. 0.15초 등장·0.25초 사라짐.
- 계기: 무드 변화(가즈아!!/물렸다…, 평온 복귀 40%), 거래소 도착, 봉 마감(마을 창에서 한 번), **개미 클릭**(대사 또는 30% 현재가),
  1.5~4분마다 혼잣말(밤엔 야근 대사), 가격 알림. 개미가 다른 모니터에 있으면 `walk-say` 로 main 을 거쳐 전달.
- 개미 클릭 반응이 생겨 호버 커서를 다시 pointer 로. 설정 `walkBubbles`(가격 알림 말풍선은 항상 표시).

### 가격 알림
- 설정 `walkAlerts: [{id, market: "btc"|"usdt", price}]`(최대 10개, 설정 창에서 추가·삭제, 돌파/이탈 대기 표시).
- 마을 창만 검사(테더 시세가 마을 창에만 있음): 직전 값과 지금 값 사이로 가격을 지나가면 `🔔 ₿ $70,000 돌파!` 말풍선 6초 +
  전광판 6초 번쩍 + 해당 거래소 타워로 질주(BTC→비트겟, 테더→업비트), 울린 알림은 지우고 저장(단일 작성자).

### 검증
- 장면 단독 하네스: 투명도 30%에서 작은 전광판 비침 없음 / 밤 그림 + "가즈아!!" 말풍선.
- 밤 버전 4채 생성 결과 확인(비트겟 벽 오인식 → 창문 위치 빌리기로 해결).
- 질주 시뮬레이션: 800px 거리 8.8초 뒤 목표 6px 이내 도착·대기, 시간 끝나면 무드 동작 복귀.
- 가격 알림 로직 추출: 첫 값 무시, 70k 돌파 1회·재발 없음, 테더 1300 이탈, 저장 목록 갱신.
- 설정 창 하네스 렌더 확인. Electron 실행(메뉴·창·실제 알림)은 미검증.

## 후속: 산책 모드 테스트 버튼을 마을 설정 창으로 (같은 0.17.41, 미배포)
- 원인: 질주·말풍선 확인 방법이 "패널 개발자 모드 Pump/Dump" 였는데 산책 모드는 패널을 쓰지 않음.
- 수정: 마을 설정 창에 **테스트** 섹션(`#devSection`, 우클릭 메뉴 "개발자 모드" 켰을 때만): 급등/급락/평온/봉 마감/가격 알림.
  기존 `pet-test` IPC 재사용, main 허용 목록에 `alert` 추가(v1 펫 창은 모르는 동작이라 무시).
  walker-app.js: `alert` = 현재가를 천 달러 단위로 반올림한 가격 "돌파! (테스트)" 말풍선·전광판 번쩍·비트겟 질주.
  봉 마감·가격 알림 테스트는 마을 창에서만 처리(모니터 여러 개일 때 말풍선 중복 방지).
- 패널 개발자 모드 버튼 → 산책 창 연결은 해가 없어 유지.

## 후속: 개발자 모드 켜면 패널까지 열리던 버그 (같은 0.17.41, 미배포)
- 원인: 개발자 모드 체크 시 테스트 버튼을 보여 주려고 **패널을 자동으로 여는 v1 코드**가 산책 모드에서도 동작 → 마을 설정 창과 패널이 둘 다 뜸.
- 수정(main.js 메뉴): 켤 때 산책 모드면 `openWalkSettings()`(테스트 섹션이 있는 곳), 펫 모드면 기존대로 패널.

## 후속: 캐릭터·건물 제작 파이프라인 (같은 0.17.41, 미배포)

### 원인
- 출근개미·건물 제작 때 프롬프트를 매번 채팅으로 새로 쓰고, 동작마다 변환 명령을 따로 치고, 문제를 눈으로만 찾았으며,
  앱 코드에 `chulgeun`·`home/office/upbit/bitget` 이 박혀 있어 새 캐릭터·건물을 넣으려면 코드를 고쳐야 했음.

### 수정
1. **레시피**: `art-src/characters/<key>/character.json`(동작별 원본·격자·fps·speed·ground·rowsRepeat),
   `art-src/town/town.json`(sheets·buildings{label,units,sign,board,nightMaskFrom,noWindows}·order).
   원본 폴더 `art-src/chulgeun` → `art-src/characters/chulgeun` 으로 이동.
2. **`tools/build_assets.py`**: 레시피대로 전부 변환 → `img/walk/<key>/`, `img/walk/characters.js|json`, `img/town/*`, `img/town/town.js`
   (+`BTCPET_TOWN_ORDER`). 출력 폴더는 매번 비우고 새로 만듦. `--only <key>` / `--town` / `--characters`.
   `sprite_slice.py`·`town_slice.py` 는 CLI 유지 + `slice_sheet()`·`slice_buildings()` 함수로 분리(품질 측정값 반환).
3. **품질 검사(경고)**: 칸별 머리 폭 차이 >8% / 마젠타 잔여 >0.3% / 이동 동작의 윗줄·아랫줄 실루엣 평균 겹침 >94%
   (실측: 같은 다리만 앞이던 달리기 v1·v2 95~96%, 승인 걷기 90%·터덜 93% — 경계가 가까워 확인 권장 수준) /
   프레임 간 차이 없음 / 전광판·간판 자리 못 찾음 / 밤 창문 비율 >28%(비트겟 무빌리기 34.7% 로 검증) 또는 <0.5%.
   팔이 튀는 문제는 실루엣으로 못 잡음(달리기 v3 경고 없음) → 미리보기 확인 필수.
4. **`tools/new_character.py` / `tools/new_building.py`**: 폴더·레시피 틀·README + 단계별 프롬프트 생성.
   템플릿 `tools/prompts/character/00~05_*.md`, `_common.md`(FIXED PARTS 포함), `tools/prompts/building.md`
   (글자 금지·하늘색 유리창·크림 간판·어두운 회색 전광판 규칙 이유 포함). 건물은 town.json 에 1칸 시트로 등록.
5. **앱이 목록을 읽음**:
   - walker.html `img/walk/characters.js` 로드, walk-scene.js `setCharacter()`, 설정 `walkCharacter`(없으면 첫 캐릭터).
   - 건물 순서 `BTCPET_TOWN_ORDER`, 간판 `signText` 의 `{company}`·`{exchange}` 치환, 전광판 종류 `board`(btc/usdt)로
     전광판 내용·질주·가격 알림 대상 결정(`boardBuildings()`).
   - 마을 설정 창: 건물 이름표를 town.js 에서, 캐릭터 2명 이상이면 캐릭터 선택 섹션. main 메뉴 "산책 캐릭터"(characters.json, 2명 이상).
   - walk-preview.html: `?char=<key>`(기본) / `?dir=`(시험 변환).
6. `tools/README.md` 에 사용법·레시피 필드·경고 대응표.

### 검증
- `build_assets.py` 전체: 출근개미 4동작·건물 4채 변환, 오류 0·경고 0.
- 버린 그림으로 검사: 달리기 v1·v2 "윗줄·아랫줄 같음" 경고, 비트겟 창문 빌리기 없이 34.7% → 밤 경고 기준 초과.
- `new_character.py zztest …` / `new_building.py zzcafe …` 실행해 프롬프트 치환($ 잔여 0)·town.json 등록 확인 후 원복·삭제.
- 하네스: 산책 창(간판·밤 자동), 마을 설정 창(이름표·테스트 섹션), 미리보기 `?char=chulgeun` 렌더 확인.

## 다음 후보
- 건물 표시 끄기/크기, 개미 크기 설정(띠 높이 연동)
- 개미가 건물에 들어가고 나오기(문 열린 그림·들어가기 동작), 출퇴근 시간 연동, 밤 버전
- 전체화면 앱 감지 시 숨김, 멀티모니터 선택
