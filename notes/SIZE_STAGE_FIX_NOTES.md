# 0.17.9 size/stage fix

- 기존 캐릭터별 `--ilsize` 보정값은 유지합니다.
- 사용자 펫 크기는 `--pet-user-scale`로 별도 적용합니다.
- 스케일은 애니메이션이 실행되는 `.pet` 바깥의 `.pet-stage`에 적용하여 기존 keyframes와 충돌하지 않습니다.
- 캐릭터 무대 기준선을 가격 HUD 위 58px로 고정하여 캐릭터별 PNG 크기 차이로 인한 하단 겹침을 방지합니다.
- 메뉴 크기 72 / 90 / 110px은 각각 0.8 / 1.0 / 1.222... 배율로 모든 캐릭터에 동일하게 적용됩니다.
