# 0.17.10 image path fix

- Fixed CSS image URLs after moving character styles into the `styles/` directory.
- Changed character asset references from `img/...` to `../img/...`.
- No animation keyframes, timing, transforms, character scale values, or layout values were changed.
